<%# Locals: status (string) %>
<% 
  color_classes = case status
    when "verified" then "bg-green-100 text-green-800"
    when "flagged" then "bg-red-100 text-red-800"
    else "bg-yellow-100 text-yellow-800"
  end 
%>
<span class="text-xs font-medium px-2.5 py-0.5 rounded <%= color_classes %>">
  <%= status.humanize %>
</span>