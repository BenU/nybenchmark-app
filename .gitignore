# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# Temporary files generated by your text editor or operating system
# belong in git's global ignore instead:
# `$XDG_CONFIG_HOME/git/ignore` or `~/.config/git/ignore`

# Ignore bundler config.
/.bundle

# Ignore all environment files.
/.env*

# Ignore all logfiles and tempfiles.
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# Ignore pidfiles, but keep the directory.
/tmp/pids/*
!/tmp/pids/
!/tmp/pids/.keep

# Ignore storage (uploaded files in development and any SQLite databases).
/storage/*
!/storage/.keep
/tmp/storage/*
!/tmp/storage/
!/tmp/storage/.keep

/public/assets

# Ignore generated sitemaps (uploaded to DO Spaces, not needed locally)
/public/sitemap*.xml.gz
/public/sitemaps/

# Ignore key files for decrypting credentials and more.
/config/*.key

# ... (Keep everything you already have) ...

# Ignore Kamal secrets (CRITICAL for security)
.kamal/secrets-common
.kamal/secrets

# Ignore Mac system files
.DS_Store

# Ignore bulk data downloads (large zip/mdb/xlsx files, re-downloadable from source)
/db/seeds/nysed_data/
/db/seeds/osc_data/county_all_years/

# Ignore local database files if you use SQLite in dev
/db/*.sqlite3
/db/*.sqlite3-*
/app/assets/builds/*
!/app/assets/builds/.keep
